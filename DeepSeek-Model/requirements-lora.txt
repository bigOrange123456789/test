# Install a CUDA-enabled torch build for your GPU if possible.
# See the PyTorch install selector for the exact CUDA index URL.
torch>=2.1.0
transformers>=4.44.0
peft>=0.11.0
accelerate>=0.33.0
safetensors>=0.4.3
